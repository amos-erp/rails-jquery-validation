require 'spec_helper'

describe Rails::Jquery::Validation do
  it 'has a version number' do
    expect(Rails::Jquery::Validation::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(true).to eq(true)
  end
end
